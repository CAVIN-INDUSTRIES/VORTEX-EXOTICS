import type { AcquisitionProfile, VehicleRecommendation } from "@/acquisition/types/contracts";

export function calculateDepreciation(_profile: AcquisitionProfile, _vehicle: VehicleRecommendation): number {
  // Phase 1 skeleton: replace with real market + trim-level depreciation logic.
  return 0;
}
