import type { AcquisitionProfile, RecommendationRow } from "@/acquisition/types/contracts";

export function emotionalMatch(_profile: AcquisitionProfile, _rows: RecommendationRow[]): RecommendationRow[] {
  // Phase 1 skeleton: returns rows unchanged until emotional model is connected.
  return _rows;
}
