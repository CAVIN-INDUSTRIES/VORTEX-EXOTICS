import type { AcquisitionProfile, VehicleRecommendationItem } from "@/acquisition/types/contracts";

export function marketRisk(_profile: AcquisitionProfile, _vehicles: VehicleRecommendationItem[]): number {
  return 0;
}
