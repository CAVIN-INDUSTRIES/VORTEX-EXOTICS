# Version pins for 3D stack: root package.json → `pnpm.overrides` (three, postprocessing, @react-three/postprocessing).
packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: false
  prisma: true
